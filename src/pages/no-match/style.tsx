import styled from 'styled-components'

export const NotFound = styled.div`
  width: 100%;
  max-width: 800px;

  img {
    width: 100%;
    height: auto;
  }

  p {
    font-size: 1.5rem;
    font-weight: bold;
    text-align: center;
    color: var(--primary-color);
  }
`
